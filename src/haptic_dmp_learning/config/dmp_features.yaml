regression:
  method: "ridge"   # "independent_lwr" (default, comportamento originale) | "ridge"
  ridge_lambda: 1.0e-6        # usato solo se method == "ridge"

second_order_canonical_system: false

velocity_filter:
  enabled: true
  window_sec_1: 0.20
  window_sec_2: 0.20